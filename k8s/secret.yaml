apiVersion: v1
kind: Secret
metadata:
  name: ccl-secret
  namespace: cloudcostlens
type: Opaque
stringData:
  # IMPORTANT: In a real production scenario, this should be injected securely via CI/CD
  # Replace this value with your actual Supabase DB password if running manually
  SPRING_DATASOURCE_PASSWORD: Devops@99ops
